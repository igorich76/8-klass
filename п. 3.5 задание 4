##
var (a,b):= (1,2);
var c:=true;
while a < b do
    c:= a = b;
Print(c);
// в общем, работает код, но смысла в этой программе нет. Особенность в том, что цикл
// не может быть завершён, он бесконечен при a < b.
